---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-world-app3
  namespace: default
  labels:
    language: ruby
  annotations:
    fluxcd.io/automated: "true"
spec:
  selector:
    matchLabels:
      app: hello-world-app3
  replicas: 2
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: hello-world-app3
    spec:
      containers:
      - name: hello-world-app3
        image: lenfree/hello-world:0.0.8
        ports:
        - containerPort: 8080
        resources:
          requests:
            cpu: "250m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: hello-world-3
  namespace: default
  labels:
    name: hello-world-3
spec:
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
  selector:
    app: hello-world-3
  type: ClusterIP
