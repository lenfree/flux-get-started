---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-world-app2
  namespace: default
  labels:
    language: ruby
  annotations:
    fluxcd.io/automated: "true"
spec:
  selector:
    matchLabels:
      app: hello-world-app2
  replicas: 2
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: hello-world-app2
    spec:
      containers:
      - name: hello-world-app2
        image: lenfree/hello-world:0.0.6
        ports:
        - containerPort: 8080
        resources:
          requests:
            cpu: "250m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: hello-world-2
  namespace: default
  labels:
    name: hello-world-2
spec:
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
  selector:
    app: hello-world-2
  type: ClusterIP
